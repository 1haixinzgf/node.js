
const readline = require('readline')


const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
})
const question = ['作者是:', '版本号']

// question.forEach (item => {
//     rl.question(item, (answer) => {
//         rl.question(item, (answer) => {
//             console.log()
//         })
//     })
// })

function question (item, ) {
    question()
}
    
